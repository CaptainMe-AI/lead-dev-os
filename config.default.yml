# DEPRECATED: This config file is no longer used.
# lead-dev-os is now a Claude Code plugin. Stack selection is handled
# interactively by the /lead-dev-os:init skill.
# See README.md for migration instructions.

version: 1.0
current_profile: default

# Tech stack profiles — controls which standards get installed
# Copy to config.local.yml and customize your profile
profiles:
  default:
    stack:
      # Global
      global: true

      # Backend
      backend: true
      # Testing
      testing: true

      # # Backend frameworks
      # fastapi: true
      # django: true
      # rails: true
      # express: true

      # # Databases
      # postgresql: true
      # mysql: true
      # mongodb: true
      # redis: true

      # # Frontend
      # react: true
      # nextjs: true
      # vue: true

      # # Infrastructure
      # nginx: true
      # gunicorn: true
      # uvicorn: true
      # docker: true

      # # Languages
      # python: true
      # ruby: true
      # typescript: true
      # javascript: true

    plan_mode:
      step1_write_spec: true
      step2_scope_tasks: true
      step3_implement_tasks: true
